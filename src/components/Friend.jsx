const Friend = ({ name, age }) => {
  // console.log(props);
  // const name = props.name;
  // const age = props.age;
  return (
    <>
      <h2>My friend's name is {name}!</h2>
      <p>Their age is {age}</p>
    </>
  );
};
export default Friend;
